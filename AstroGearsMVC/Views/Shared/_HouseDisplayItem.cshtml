@model string[,]

@for (var i = 0; i < Model.GetLength(0); i++)
{
    <div class="col-sm-6 col-md-3">
        <label class="col-sm-6 col-md-4">@(Model[i, 1])</label>
        <div id="@(Model[i, 0])" class="col-sm-6 col-md-8"></div>
    </div>
}